

<!DOCTYPE html>

<html>
<head>

  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  
</head> 

<body>
	<p id="notice"><%= notice %></p>

    <div class="container">
      <table class="table table-hover">
        <thead>
          <tr>
       <th >Price</th>
      <th >Description</th>
      <th >Quantity</th>
     


           
          </tr>
        </thead>
        <tbody>
            <tr>
              <td><%= @line_item.price %></td>
              <td><%= @line_item.description %></td>
              <td><%= @line_item.quantity %></td>
              
            

            </tr>   
        </tbody>
      </table>
      <%= link_to 'Edit', edit_invoice_line_item_path(params[:invoice_id], @line_item), class: "btn btn-info" %>
    </div>
</body>
</html>

